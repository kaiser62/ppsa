# =============================================================================
# PPSA - Main Docker Stack (Phase 2: hardened)
# =============================================================================

volumes:
  palworld_data:
    labels:
      - "ppsa.volume=palworld"
      - "docker-volume-backup.stop-during-backup=true"
  wgdashboard_data:
    labels:
      - "ppsa.volume=wgdashboard"
      - "docker-volume-backup.stop-during-backup=true"
  webui_data:
    labels:
      - "ppsa.volume=webui"
      - "docker-volume-backup.stop-during-backup=true"

# ponytail: single default network. Per-service network isolation adds complexity
# for negligible security gain behind UFW. Add if multi-tenant needed.

services:
  # ---------------------------------------------------------------------------
  # Palworld Dedicated Server
  # ---------------------------------------------------------------------------
  palworld:
    image: thijsvanloef/palworld-server-docker:latest
    container_name: ppsa-palworld
    restart: unless-stopped
    stop_grace_period: 120s    # generous for world save
    ports:
      - "8211:8211/udp"
      - "27015:27015/udp"
    volumes:
      - palworld_data:/palworld
    environment:
      PUID: 1000
      PGID: 1000
      PORT: 8211
      PLAYERS: ${MAX_PLAYERS:-16}
      SERVER_PASSWORD: ${SERVER_PASSWORD:-}
      SERVER_NAME: ${SERVER_NAME:-PPSA Server}
      SERVER_DESCRIPTION: ${SERVER_DESCRIPTION:-Managed by PPSA}
      ADMIN_PASSWORD: ${ADMIN_PASSWORD:-changeme}
      MULTITHREADING: ${MULTITHREADING:-true}
      RCON_ENABLED: ${RCON_ENABLED:-true}
      RCON_PORT: ${RCON_PORT:-25575}
      RESTAPI_ENABLED: ${RESTAPI_ENABLED:-true}
      RESTAPI_PORT: ${RESTAPI_PORT:-8212}
      COMMUNITY: ${COMMUNITY:-false}
      TZ: ${TZ:-UTC}
    healthcheck:
      test: ["CMD", "rcon-cli", "Info"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 120s
    deploy:
      resources:
        limits:
          cpus: "2.0"   # 2-CPU VMs need ≤2; bigger hosts also fine
          memory: 4G    # 4GB cap works on 4GB+ hosts; adjust per-machine
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "ppsa.component=palworld"

  # ---------------------------------------------------------------------------
  # Web Management UI
  # ---------------------------------------------------------------------------
  webui:
    build:
      context: ../docker/webui
      dockerfile: Dockerfile
    container_name: ppsa-webui
    restart: unless-stopped
    ports:
      - "8080:8080"   # UFW restricts to authorized IPs; LAN access by default
    volumes:
      - webui_data:/app/data
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /etc/wireguard:/etc/wireguard:rw
      - ../backups:/backups:ro
    cap_add:
      - NET_ADMIN
    environment:
      TZ: ${TZ:-UTC}
      PALWORLD_API_URL: http://palworld:${RESTAPI_PORT:-8212}
      PALWORLD_ADMIN_PASSWORD: ${ADMIN_PASSWORD:-changeme}
      DATA_DIR: /app/data
    depends_on:
      palworld:
        condition: service_healthy
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8080/health"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "ppsa.component=webui"

  # ---------------------------------------------------------------------------
  # WireGuard Dashboard
  # ---------------------------------------------------------------------------
  wgdashboard:
    image: ghcr.io/wgdashboard/wgdashboard:latest
    container_name: ppsa-wgdashboard
    restart: unless-stopped
    ports:
      - "10086:10086"  # UFW restricts to authorized IPs
    volumes:
      - wgdashboard_data:/opt/wireguarddashboard/app/db
      - /etc/wireguard:/etc/wireguard:ro
    cap_add:
      - NET_ADMIN
    sysctls:
      - net.ipv4.ip_forward=1
      - net.ipv6.conf.all.forwarding=1
    environment:
      TZ: ${TZ:-UTC}
    healthcheck:
      # ponytail: Python urllib over wget — Python slim images lack wget/curl
      test: ["CMD-SHELL", "python3 -c \"from urllib.request import urlopen; urlopen('http://localhost:10086')\""]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 256M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "ppsa.component=wireguard"

  # ---------------------------------------------------------------------------
  # Backup Agent (offen/docker-volume-backup v2)
  # ---------------------------------------------------------------------------
  backup:
    image: offen/docker-volume-backup:v2
    container_name: ppsa-backup
    restart: unless-stopped
    volumes:
      - palworld_data:/backup/palworld_data:ro
      - webui_data:/backup/webui_data:ro
      - wgdashboard_data:/backup/wgdashboard_data:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ../backups:/archive
    environment:
      BACKUP_CRON_EXPRESSION: ${BACKUP_SCHEDULE:-0 3 * * *}
      BACKUP_RETENTION_DAYS: ${BACKUP_RETENTION_DAYS:-7}
      BACKUP_PRUNING_PREFIX: backup-
      BACKUP_FILENAME: "ppsa-{{ .ContainerName }}-{{ .Now | date \"20060102-150405\" }}.tar.gz"
      NOTIFY_URLS: ${DISCORD_WEBHOOK_URL:-}
    # ponytail: Go static binary on scratch — no shell/tools available for health check.
    # Container restart policy handles process death. Add health check if upstream
    # image adds HTTP endpoint.
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 512M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "ppsa.component=backup"

  # ---------------------------------------------------------------------------
  # Watchtower (Automatic Updates)
  # ---------------------------------------------------------------------------
  watchtower:
    image: containrrr/watchtower:latest
    # ponytail: using containrrr/watchtower; switch to nicholas-fedor/watchtower if Docker 29+ issues arise
    container_name: ppsa-watchtower
    restart: unless-stopped
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    environment:
      WATCHTOWER_CLEANUP: "true"
      WATCHTOWER_SCHEDULE: "0 4 * * *"
      WATCHTOWER_LABEL_ENABLE: "true"
      WATCHTOWER_LABELS: "ppsa.component"
      WATCHTOWER_INCLUDE_RESTARTING: "false"
    healthcheck:
      test: ["CMD", "pgrep", "-f", "watchtower"]
      interval: 5m
      timeout: 5s
      retries: 2
      start_period: 15s
    deploy:
      resources:
        limits:
          cpus: "0.25"
          memory: 128M
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    labels:
      - "ppsa.component=watchtower"
