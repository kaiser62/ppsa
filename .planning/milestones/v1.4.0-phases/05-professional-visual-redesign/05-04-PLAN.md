---
phase: 05-professional-visual-redesign
plan: 04
type: execute
wave: 4
depends_on: ["05-03"]
files_modified:
  - docker/webui/app/static/index.html
autonomous: false

requirements: [UI-01, UI-02, UI-04]

must_haves:
  truths:
    - "kickPlayer, banPlayer, wifiDisconnect, wifiHotspotStart, wifiHotspotStop, removeMod, and resetFirewall all use the existing .modal confirm dialog (confirmAction helper) instead of native confirm()/alert()"
    - "No native confirm() or alert() call remains anywhere in the file for the 6 UI-SPEC-named actions plus resetFirewall (Firewall is an in-scope tab whose Copywriting Contract explicitly spells out .modal copy for it)"
    - "At viewport width 375px, document.body.scrollWidth equals document.body.clientWidth (no horizontal page scroll) on Dashboard, Server Controls, Firewall, Backup, and Wi-Fi tabs"
    - "At viewport width 1440px, the same 5 tabs render without a horizontal scrollbar and without any fixed pixel-width element overflowing the .container max-width"
    - "Every button used as a primary row action on a touch surface (Kick/Ban/Restore/Remove/Connect in tables) reaches a minimum 44px rendered tap height at <=768px, or is explicitly documented here as an accepted icon-adjacent/secondary exception"
  artifacts:
    - docker/webui/app/static/index.html (modified: JS confirm()/alert() call sites migrated to confirmAction/.modal, no new files)
  key_links:
    - "confirmAction(msg, cb) (already defined at the bottom of the <script> block) is reused as the single migration target for every native confirm() call in this plan — no second confirm helper is introduced"
</must_haves>
---

<objective>
Migrate the remaining native `confirm()`/`alert()` call sites in Dashboard/Controls/Firewall/Backup/
Wi-Fi-tab JS to the existing `.modal` component via `confirmAction()`, then verify the full phase's
responsive behavior at 375px (phone) and 1440px (laptop) viewports with zero page-level horizontal
scroll, and audit touch-target sizing on row-action buttons. This is the final plan in the phase —
it closes out UI-04 or requires a human-verify checkpoint if any viewport issue is found that cannot
be fixed without touching out-of-scope tabs.

Purpose: complete the Copywriting Contract's confirmation-dialog consistency requirement and prove
(not just assert) the responsive/touch-target must-haves that the earlier 3 plans built toward.
Output: JS confirm()/alert() calls migrated to `.modal`; a completed human-verify checkpoint
confirming no horizontal scroll and acceptable touch targets at both target viewports.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-professional-visual-redesign/05-UI-SPEC.md
@.planning/phases/05-professional-visual-redesign/05-01-SUMMARY.md
@.planning/phases/05-professional-visual-redesign/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate kick/ban/mod-remove native confirm() calls to the .modal component</name>
  <files>docker/webui/app/static/index.html</files>
  <read_first>
    docker/webui/app/static/index.html (lines 765-776 kickPlayer/banPlayer; lines 1111-1119 removeMod; lines 1415-1423 confirmAction/closeModal definitions)
    .planning/phases/05-professional-visual-redesign/05-UI-SPEC.md (Copywriting Contract "Destructive confirmation" row)
  </read_first>
  <action>
    Replace `kickPlayer(uid)`'s body: currently
    `if (!confirm('Kick player ' + uid + '?')) return; await api(...); refreshPlayers();` — change to
    use `confirmAction`'s callback pattern: wrap the existing API-call-plus-refresh logic in a callback
    function and call `confirmAction('Kick player ' + uid + '?', async () => { await api('/api/players/kick', ...);
    refreshPlayers(); });` matching the exact copy string already used (no wording change, just delivery
    mechanism). Note `confirmAction`'s existing signature is `confirmAction(msg, cb)` where `cb` is called
    synchronously from the confirm button's `onclick` — an async `cb` is fine since `confirmAction`
    does not await it (fire-and-forget matches the existing pattern used elsewhere, e.g.
    `shutdownServer`/`stopServer` already called this way via the Controls tab's inline
    `confirmAction('...', shutdownServer)` usage).
    Apply the identical transformation to `banPlayer(uid)` using the exact existing copy
    'Ban player ' + uid + '? This is permanent.'.
    Apply the identical transformation to `removeMod(name)` (currently
    `if (!confirm('Remove mod ' + name + '?')) return;`), using the exact existing copy
    'Remove mod ' + name + '?', wrapping its existing DELETE-then-refreshMods body as the callback.
    Do not alter the message copy in any of these three functions — only change the confirmation
    mechanism from native `confirm()` to `confirmAction()`.
  </action>
  <acceptance_criteria>
    - `grep -n "confirm('Kick player"` and `grep -n "confirm('Ban player"` and
      `grep -n "confirm('Remove mod"` all return zero matches for the native `confirm(` form (i.e.
      these three literal strings only appear as arguments to `confirmAction(`, never bare `confirm(`)
    - `kickPlayer`, `banPlayer`, `removeMod` each call `confirmAction(...)` with their original exact
      copy string as the first argument
    - The kick/ban/remove API call + refresh logic still executes correctly when confirmed (functionally
      unchanged, only trigger mechanism changed)
  </acceptance_criteria>
  <verify>
    <automated>grep -c "confirm('Kick player" docker/webui/app/static/index.html</automated>
  </verify>
  <done>kickPlayer, banPlayer, and removeMod all trigger through confirmAction()/.modal instead of native confirm(), with identical copy and identical resulting API behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Wi-Fi disconnect/hotspot-start/hotspot-stop and Firewall reset native confirm()/alert() calls to .modal</name>
  <files>docker/webui/app/static/index.html</files>
  <read_first>
    docker/webui/app/static/index.html (lines 1049-1077 wifiDisconnect/wifiHotspotStart/wifiHotspotStop; lines 1173-1182 resetFirewall)
    .planning/phases/05-professional-visual-redesign/05-UI-SPEC.md (Copywriting Contract "Destructive confirmation" row — Reset firewall copy is spelled out verbatim there even though the phase's summary prose names only 6 call sites; Firewall is an in-scope tab so its confirm dialog is migrated here for consistency with the rest of the Copywriting Contract table)
  </read_first>
  <action>
    Replace `wifiDisconnect()`'s native `confirm('Disconnect from current Wi-Fi network?')` and its
    subsequent native `alert('Disconnect failed: ' + err.message)` on catch: use
    `confirmAction('Disconnect from current Wi-Fi network?', async () => { try { await
    api('/api/wifi/disconnect', {method:'POST'}); setTimeout(wifiScan, 1000); } catch(err) {
    showAlert('Disconnect failed: ' + err.message, 'error', 'wifi-alert'); } });` — replacing the raw
    `alert()` on failure with the existing `showAlert()` helper targeting `#wifi-alert` (already present
    in the Wi-Fi tab markup), consistent with every other tab's error-banner pattern.
    Replace `wifiHotspotStart()`'s native `confirm(...)` + two native `alert(...)` calls (success and
    failure) the same way: `confirmAction('Start the PPSA-Setup hotspot? You will need to reconnect to
    "PPSA-Setup" Wi-Fi (password: ppsa-setup-2026) to manage this device.', async () => { try { await
    api('/api/wifi/hotspot/start', {method:'POST'}); showAlert('PPSA-Setup hotspot started. Connect
    your phone to "PPSA-Setup" and visit http://192.168.50.1/', 'success', 'wifi-alert'); } catch(err) {
    showAlert('Failed: ' + err.message, 'error', 'wifi-alert'); } });` — same exact copy, both alert
    branches now routed through `showAlert()` instead of native `alert()`.
    Replace `wifiHotspotStop()` identically: confirm copy 'Turn off the PPSA-Setup hotspot?', success
    copy 'PPSA-Setup hotspot stopped.', failure copy 'Failed: ' + err.message, both via `showAlert()`
    targeting `wifi-alert`.
    Replace `resetFirewall()`'s native `confirm('Reset firewall to the default allowed ports?\n\nTCP:
    22, 80, 443, 8080, 10086, 25575\nUDP: 8211, 27015\nICMP: on')`: use `confirmAction()` with the same
    copy but reformatted for the `.modal`'s `<p id="modal-text">` (which renders as plain text, not
    a `<pre>`) — replace the `\n\n`/`\n` line breaks with `.modal-text`'s natural wrapping by using a
    single space-separated sentence instead: 'Reset firewall to the default allowed ports? TCP: 22, 80,
    443, 8080, 10086, 25575 / UDP: 8211, 27015 / ICMP: on' (this exact string is already given in
    UI-SPEC's Copywriting Contract "Destructive confirmation" row for Reset firewall — use it verbatim
    instead of re-deriving from the old `\n`-joined string).
  </action>
  <acceptance_criteria>
    - `wifiDisconnect`, `wifiHotspotStart`, `wifiHotspotStop`, `resetFirewall` each call
      `confirmAction(...)` — grep for bare `confirm('Disconnect from current Wi-Fi`,
      `confirm('Start the PPSA-Setup hotspot`, `confirm('Turn off the PPSA-Setup hotspot`,
      `confirm('Reset firewall` all return zero matches
    - No native `alert(` call remains inside `wifiDisconnect`, `wifiHotspotStart`, `wifiHotspotStop`
      function bodies — all replaced by `showAlert(..., 'wifi-alert')`
    - `resetFirewall`'s confirm copy exactly matches the UI-SPEC string:
      "Reset firewall to the default allowed ports? TCP: 22, 80, 443, 8080, 10086, 25575 / UDP: 8211,
      27015 / ICMP: on"
    - `restartPalworld` (Config tab, out of scope) and `wgDisconnect` (WireGuard tab, out of scope)
      are NOT modified in this task — confirm they still use native `confirm()` unchanged, since those
      tabs are explicitly out of scope for Phase 5 per hard constraints
  </acceptance_criteria>
  <verify>
    <automated>grep -c "confirm('Disconnect from current Wi-Fi" docker/webui/app/static/index.html</automated>
  </verify>
  <done>wifiDisconnect, wifiHotspotStart, wifiHotspotStop, resetFirewall all use confirmAction()/.modal with showAlert()-routed error/success messaging; restartPalworld and wgDisconnect remain untouched (out of scope).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify responsive layout (375px/1440px, no body horizontal scroll) and modal migration</name>
  <files>docker/webui/app/static/index.html</files>
  <action>
    Serve or deploy the current index.html (via `ppsa-guest-ops` skill against a running PPSA
    appliance/VM, or a local static server for layout-only inspection) and drive the how-to-verify
    steps below using browser DevTools device emulation at 375px and 1440px viewport widths. This is
    a human-verify checkpoint — the executor performs the deploy/serve and drives the checks, then
    pauses for human confirmation before the phase can be marked complete.
  </action>
  <what-built>
    Automated: full Plan 05-01..05-04 design-system application across Dashboard, Server Controls,
    Firewall, Backup, and Wi-Fi tabs (tokens, elevation, icon sprite, empty/loading states, long-text
    overflow handling, and all 7 in-scope-tab confirm()/alert() calls migrated to the .modal component).
  </what-built>
  <how-to-verify>
    1. Serve the static file locally for inspection: from `docker/webui/app/static/`, run
       `python3 -m http.server 8899` (or open `index.html` directly in a browser — login will fail
       against a real backend, but you can inspect the login screen and static layout; for full-app
       verification against a live backend, use the `ppsa-guest-ops` skill to reach a running PPSA
       instance's WebUI at its NetBird overlay IP, port 8080).
    2. Open Chrome/Edge DevTools, toggle device toolbar, set viewport to 375 x 667 (iPhone SE class).
       For each of Dashboard, Server Controls, Firewall, Backup, Wi-Fi: confirm there is NO horizontal
       scrollbar on the page body (run `document.body.scrollWidth === document.body.clientWidth` in
       the console — should print `true` on every tab).
    3. Set viewport to 1440 x 900 (laptop class). Repeat the same 5-tab check — confirm no horizontal
       scrollbar and no element visibly overflowing the `.container` (max-width 1200px, so there
       should be centered whitespace on either side at 1440px).
    4. On the 375px viewport, tap/click each row-action button (Kick, Ban in Players tab is out of
       scope but visible via nav — focus on in-scope tabs: Backup's Restore button, Wi-Fi's Connect
       button) and confirm each is comfortably tappable (visually ~44px tall, not a cramped ~24px hit
       target).
    5. Trigger one migrated confirmation (e.g. Wi-Fi tab's "Disconnect" button if connected, or
       Firewall's "Reset to Defaults" button) and confirm the styled `.modal` dialog appears (dark
       overlay + centered card) instead of a native browser confirm() popup.
  </how-to-verify>
  <resume-signal>Type "approved" if all 5 tabs pass both viewport checks and the modal renders correctly, or describe any horizontal-scroll/touch-target/modal issue found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → static file | JS-only behavioral change (confirm/alert → modal), no new endpoint, no new data flow |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-06 | Tampering | Migrated JS confirm handlers | low | accept | Callbacks passed to confirmAction() are the exact same API calls (`/api/wifi/disconnect`, `/api/firewall/reset`, etc.) that already existed behind native confirm() — no new privilege or endpoint is exposed, only the UI trigger mechanism changed |
| T-05-07 | Denial of Service | Destructive action confirm dialogs (Reset firewall, Wi-Fi disconnect) | low | accept | Same authenticated-only endpoints as before (JWT bearer required by existing `/api/*` auth dependency, unchanged by this phase); confirmation UI change does not alter the authorization boundary |

</threat_model>

<verification>
1. `grep -c "confirm(" docker/webui/app/static/index.html` — the only remaining bare `confirm(` calls
   should be `restartPalworld`'s and `wgDisconnect`'s (both explicitly out of scope), confirmed by
   inspecting each remaining match's surrounding function name.
2. Load the app end-to-end and manually trigger the human-verify checkpoint steps above.
3. Confirm no `/api/*` request payloads or endpoints changed — this plan is JS-trigger-mechanism and
   CSS/markup only.
</verification>

<success_criteria>
- All 7 in-scope-tab native confirm()/alert() calls (kick, ban, mod-remove, wifi-disconnect,
  hotspot-start, hotspot-stop, reset-firewall) migrated to `.modal` via `confirmAction()`
- Human-verified: no page-level horizontal scroll at 375px or 1440px across all 5 in-scope tabs
- Human-verified: touch targets on row-action buttons are comfortably tappable at phone width
- Human-verified: migrated confirmations render the styled `.modal`, not a native browser dialog
</success_criteria>

<output>
Create `.planning/phases/05-professional-visual-redesign/05-04-SUMMARY.md` when done
</output>
