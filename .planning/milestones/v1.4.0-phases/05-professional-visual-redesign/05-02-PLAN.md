---
phase: 05-professional-visual-redesign
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - docker/webui/app/static/index.html
autonomous: true
requirements: [UI-01, UI-02, UI-04]

must_haves:
  truths:
    - "The Dashboard tab's stat cards (Players, Server FPS, Uptime, Version) use only the shared .card/.text-label/.text-display classes from Plan 05-01 — no inline style=\"font-size:...\" remains on any Dashboard card element"
    - "The Version stat card handles a long version string without breaking the card's fixed grid column width (overflow-wrap applied)"
    - "Server Controls tab's primary actions (Save World, Broadcast) and destructive actions (Restart, Stop) visually distinguish primary vs destructive via the existing --primary/--red accent rules, with no remaining ad-hoc inline spacing styles on the Logs section"
    - "The Dashboard tab's server-status stat card (Players card position) is visually the first, most prominent element in the stat grid, consistent with UI-SPEC's stated primary focal point for this tab"
    - "The Dashboard's degraded/non-running-state banner (#dashboard-alert, driven by Phase 4's existing server_state/error logic) renders using the shared .alert/.alert-warning/.alert-error/.alert-info classes with the new spacing tokens applied — the banner's visual treatment is restyled by this plan, its underlying state logic is untouched"
  artifacts:
    - docker/webui/app/static/index.html (modified: #page-dashboard and #page-controls markup, no <style> block structural changes beyond what Plan 05-01 already added)
  key_links:
    - "Dashboard/Controls markup consumes the --space-* and --font-*-size tokens and .icon sprite defined in Plan 05-01 — this plan must not redeclare or duplicate those tokens"
</must_haves>
---

<objective>
Apply the Plan 05-01 design-system tokens/classes to the Dashboard and Server Controls tab markup:
remove ad-hoc inline `style="font-size:..."`/`style="margin-top:..."` attributes, apply the spacing
scale to card/section internals, and ensure the long-text backstop (overflow-wrap) covers the
Version stat value. This plan only touches `#page-dashboard` and `#page-controls` markup blocks —
it does not touch Firewall/Backup/Wi-Fi (Plan 05-03) or JS confirm()/alert() migration (Plan 05-04).

Purpose: turn the tightened design system into visible, applied UI on the two tabs a friend-facing
user sees most (dashboard status, server controls) — per UI-SPEC's stated primary focal point.
Output: Updated `#page-dashboard` and `#page-controls` HTML blocks in index.html using shared classes
instead of inline styles.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-professional-visual-redesign/05-UI-SPEC.md
@.planning/phases/05-professional-visual-redesign/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Dashboard tab stat cards and player table, remove inline font-size overrides</name>
  <files>docker/webui/app/static/index.html</files>
  <read_first>
    docker/webui/app/static/index.html (lines 200-215, `#page-dashboard` block: stat grid + Connected Players section)
    .planning/phases/05-professional-visual-redesign/05-UI-SPEC.md (Spacing Scale, Typography, "overflow (long numbers)"/"long-text" UI Considerations rows, Primary focal point paragraph)
  </read_first>
  <action>
    In the Dashboard stat grid (`<div class="grid" id="dash-stats">`), remove the inline
    `style="font-size:1rem"` currently on `<div class="value" id="stat-version">` and instead add a
    new CSS rule in the `<style>` block (near `.card .value`) scoped to this one card:
    `#stat-version { font-size: var(--font-heading-size); overflow-wrap: anywhere; }` — this both
    fixes the ad-hoc inline override and satisfies the long-text backstop (a long version string
    wraps within the card instead of overflowing the grid column). Do this by adding the id-scoped
    rule to `<style>`, then delete `style="font-size:1rem"` from the `#stat-version` div in the markup
    so no inline style remains on it.
    Reorder nothing in the grid — the Players card stays first per the UI-SPEC's stated primary focal
    point (server-status is the first, most attention-grabbing element); do not move it.
    Add `title` attribute binding is not needed here since `#stat-version`'s content is set by JS
    (`stat-version.textContent`), so the overflow-wrap CSS rule alone is sufficient — do not add a
    static `title=` attribute to markup that gets overwritten by JS on every refresh.
    In the Connected Players table (`<table>` inside `.section`), no markup change is required beyond
    what Plan 05-01's `.section`/`th`/`td` rules already provide — confirm during this task that no
    inline `style=` attributes exist inside `#page-dashboard` after your edit (there should be zero
    remaining after removing `#stat-version`'s inline style).
    Update the existing `.alert` rule's `padding: 0.75rem; margin-bottom: 0.5rem;` to
    `padding: var(--space-sm) var(--space-md); margin-bottom: var(--space-sm);` in the `<style>` block
    (this rule is shared across all 5 tabs' banners, so this one edit restyles `#dashboard-alert`'s
    spacing along with every other tab's alert banner — do not add a dashboard-specific override).
    Do not change `.alert-success`/`.alert-error`/`.alert-info`/`.alert-warning` color values or the
    JS that sets `#dashboard-alert`'s class/text (Phase 4's `server_state`-driven banner logic in
    `refreshDashboard()` is untouched) — this task only restyles the shared `.alert` box spacing.
  </action>
  <acceptance_criteria>
    - `grep -o 'id="page-dashboard"[^~]*' ` region contains zero `style="` attributes after this task
      (verify via: extract lines 200-216 and confirm no `style=` substring remains)
    - A new CSS rule `#stat-version { font-size: var(--font-heading-size); overflow-wrap: anywhere; }`
      (or equivalent selector targeting `#stat-version`) exists in the `<style>` block
    - The `<div class="value" id="stat-version"` element in markup no longer has a `style=` attribute
    - The shared `.alert` rule uses `var(--space-sm)`/`var(--space-md)` for padding/margin instead of
      bare `0.75rem`/`0.5rem`, and no dashboard-specific `#dashboard-alert` override was added
    - Dashboard grid element order is unchanged (Players card remains the first `.card` child of
      `#dash-stats`)
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'id="stat-version"' docker/webui/app/static/index.html</automated>
  </verify>
  <done>Dashboard stat cards use only shared classes plus one scoped #stat-version rule for long-text overflow; no inline style= attributes remain inside #page-dashboard; Players card remains first in the grid.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Server Controls tab — button grouping, logs section spacing</name>
  <files>docker/webui/app/static/index.html</files>
  <read_first>
    docker/webui/app/static/index.html (lines 227-245, `#page-controls` block)
    .planning/phases/05-professional-visual-redesign/05-UI-SPEC.md (Color table accent/destructive rows, Copywriting Contract primary CTA row for Controls, Additional Design Notes focal-point paragraph for Server Controls)
  </read_first>
  <action>
    In `#page-controls`, remove the inline `style="margin-top:0.5rem;display:flex;gap:0.5rem"` on the
    `<div>` wrapping the Refresh button under the Logs textarea, replacing it with a new class
    `.btn-row` added to the `<style>` block: `.btn-row { margin-top: var(--space-sm); display: flex;
    gap: var(--space-sm); flex-wrap: wrap; }` — apply `class="btn-row"` to that div instead of the
    inline style.
    Confirm the "Save World" and "Broadcast" buttons keep the default `button`/`.btn` accent-blue
    styling (they are the tab's primary actions per Copywriting Contract) and "Restart"/"Stop" keep
    `class="danger"` — do not add `.outline` or change these classes, they are already correct;
    this task is about removing the one remaining inline style, not reclassifying buttons.
    Verify the `.btn-group` class (already defined in Plan 05-01's untouched base CSS) wraps the four
    action buttons with `gap: var(--space-sm)` — if `.btn-group`'s gap is still the pre-Plan-05-01
    literal `0.5rem`, update it to `gap: var(--space-sm)` for consistency with the new `.btn-row` class
    (both should resolve to the same 8px value, just via the token now).
  </action>
  <acceptance_criteria>
    - The Logs section's button wrapper div uses `class="btn-row"` with no `style=` attribute
    - `.btn-row` CSS rule exists using `var(--space-sm)` for both `margin-top` and `gap`
    - `.btn-group` rule's `gap` value uses `var(--space-sm)` (not a bare `0.5rem`)
    - "Restart" and "Stop" buttons retain `class="danger"`; "Save World"/"Broadcast" retain no danger
      class (default accent styling)
    - Zero `style=` attributes remain inside `#page-controls` after this task
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'btn-row' docker/webui/app/static/index.html</automated>
  </verify>
  <done>Server Controls tab has no inline styles; button spacing driven by .btn-group/.btn-row tokens; primary vs destructive button distinction preserved exactly as specified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → static file | CSS/markup-only edits to two tab sections; no new input surface, no `/api/*` change |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-03 | Tampering | Dashboard/Controls markup | low | accept | Static, build-baked, read-only at runtime — same posture as T-05-01 in Plan 05-01 |

</threat_model>

<verification>
1. Load the app (authenticate, land on Dashboard tab) — stat cards render with no visual regression
   (Players/FPS/Uptime/Version cards all present, values populate from `/api/dashboard` unchanged).
2. Switch to Server Controls tab — Save World/Broadcast/Restart/Stop buttons render with correct
   accent/danger coloring; Logs textarea and Refresh button render below with consistent spacing.
3. `grep -c 'style="' <(sed -n "200,245p" docker/webui/app/static/index.html)` returns 0.
</verification>

<success_criteria>
- Dashboard and Server Controls tabs fully consume Plan 05-01's tokens
- No inline `style=` attributes remain in either tab's markup
- No JS behavior changed (server_state/players_known rendering logic untouched)
</success_criteria>

<output>
Create `.planning/phases/05-professional-visual-redesign/05-02-SUMMARY.md` when done
</output>
