---
phase: 07-boot-chain-verification-hang-detection
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/ppsa-installer-e2e.py
autonomous: true
requirements: [BOOT-01, BOOT-02]
must_haves:
  truths:
    - "After a scripted install, the tester reports whether the post-install boot chain came up via signed shim/GRUB, or explicitly flags that it fell back to the unsigned path (only expected/acceptable when Secure Boot is off)"
    - "During a long-running install, the tester distinguishes real progress (recent heartbeat activity) from a genuine hang (stale/absent heartbeat past a grace threshold) via the /run/ppsa-install.activity file polled over SSH, rather than a single fixed timeout alone"
    - "A boot-chain verification failure/warning and a hang-detected timeout produce distinguishable, actionable failure reasons in the tester's output rather than one generic failed result"
  artifacts:
    - "verify_boot_chain(ssh_runner) function/method on InstallerE2ETester, querying dmesg/proc/cmdline over SSH and returning a (status, reason) classification"
    - "wait_for_install_complete() extended to poll /run/ppsa-install.activity alongside /opt/ppsa/.installed, tracking heartbeat staleness distinct from the existing SSH-unreachable/marker-absent distinction"
    - "run() calling verify_boot_chain() after a successful completion-poll and folding its result into the ordered results list and overall_pass computation, replacing the Phase 6 stub NOTE line"
  key_links:
    - "verify_boot_chain() is invoked from run() using the SAME SshRunner instance/pattern already established for wait_for_install_complete(), not a new SSH transport"
    - "wait_for_install_complete()'s existing self.last_failure_reason distinguishing mechanism (SSH-never-reachable vs marker-never-appeared) is EXTENDED with a third distinguishable reason (heartbeat-stale-hang-suspected), not replaced or collapsed into a single generic string"
    - "run()'s existing ordered (step_name, status) results list and for/else first-failure-stops pipeline pattern from Phase 6 is preserved and extended with one new step, not restructured"
---

<objective>
Extend `scripts/ppsa-installer-e2e.py` (created/completed in Phase 6) with the two
verification capabilities this phase's requirements demand: `verify_boot_chain()`
(BOOT-01) — a post-boot SSH query classifying the installed system's boot path as signed
shim/GRUB or unsigned fallback — and heartbeat-aware hang detection (BOOT-02) — extending
`wait_for_install_complete()` to poll Plan 07-01's `/run/ppsa-install.activity` heartbeat
file so a slow-but-progressing Docker pull is not misreported as a hang.

Purpose: Phase 6 explicitly deferred both capabilities to this phase (see the `run()`
stub NOTE line and Phase 6's VERIFICATION.md "Known Limitations"). This plan closes that
gap so the E2E tester can tell a genuinely broken/hung install apart from a
correctly-booted (or documented-fallback) one, instead of guessing from a single fixed
timeout — the Phase 7 ROADMAP goal verbatim.

Output: `scripts/ppsa-installer-e2e.py` extended in place with `verify_boot_chain()`,
heartbeat-aware timeout classification in `wait_for_install_complete()`, and both wired
into `run()`'s pipeline with distinguishable, actionable failure/warning reasons in the
final summary output — structured so Phase 8 can fold a third (smoke-test) result in
without another refactor.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-boot-chain-verification-hang-detection/07-RESEARCH.md
@.planning/phases/07-boot-chain-verification-hang-detection/07-01-PLAN.md

@.claude/skills/ppsa-installer-test/SKILL.md
@.claude/skills/ppsa-guest-ops/SKILL.md
@scripts/ppsa-installer-e2e.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: verify_boot_chain() post-boot SSH classification (BOOT-01)</name>
  <files>scripts/ppsa-installer-e2e.py</files>
  <read_first>
    - scripts/ppsa-installer-e2e.py lines 132-209 (SshRunner class in full: exec(), accept_host_key(), the exit_code==-1 connection-failure convention — verify_boot_chain MUST reuse this exact class/instance, not a new transport)
    - scripts/ppsa-installer-e2e.py lines 649-719 (InstallerE2ETester.run(): the existing steps list, for/else pipeline, results-list pattern, and the Phase-6-stub NOTE print at lines 706-710 that this task's wiring REPLACES)
    - .planning/phases/07-boot-chain-verification-hang-detection/07-RESEARCH.md "Pattern 2: Post-Boot Secure Boot Chain Detection" and "Pitfall 1: Assuming Signed Shim/GRUB When Unsigned Fallback Is Taken" and "Pitfall 4: Unsigned Fallback Is Correct for Some Builds" (the exact dmesg/proc/cmdline query strings and the classify-don't-assume rule)
    - CLAUDE.md "Boot chain (Secure Boot matters here)" section (why /EFI/debian prefix is immutable, why fbx64.efi is deliberately omitted, and the unsigned grub-mkstandalone fallback semantics this classification must distinguish)
  </read_first>
  <action>
    Add a `verify_boot_chain(self, ssh_target)` method on `InstallerE2ETester` (placed
    after `wait_for_install_complete()`, before `run()`), that:

    1. Constructs (or reuses, if already available on `self`) an `SshRunner` instance
       targeting `ssh_target` with `self.ssh_password` and `self.verbose`, matching the
       exact construction already used in `wait_for_install_complete()` — do not
       introduce a second SSH transport class or duplicate the plink/ssh
       auto-detection logic.

    2. Runs `dmesg | grep -iE 'secure.boot|shim' | head -5` over SSH (timeout 10s). If
       `exit_code == 0` and stdout is non-empty, classify as `("PASS", "Signed
       Shim/GRUB (Secure Boot chain markers found in dmesg)")`.

    3. If step 2 found nothing, falls back to `cat /proc/cmdline` (timeout 10s) and
       checks the returned stdout (case-insensitively) for `"efi"` or `"secure"`. If
       found, classify as `("PASS", "EFI/Secure Boot keyword found in /proc/cmdline")`.

    4. If neither query finds signed-boot markers (exit_code == 0 but no matching
       content, or the connection itself failed), classify as `("WARN", "No signed
       shim/GRUB markers found via dmesg or /proc/cmdline -- likely unsigned
       grub-mkstandalone fallback (expected/acceptable only when Secure Boot is off;
       see CLAUDE.md Boot chain section)")`. Per Pitfall 4 in the research, this
       function must NOT declare this a FAIL -- absence of markers is a documented,
       sometimes-intentional fallback, not automatically a regression. It classifies
       and reports; it does not judge build intent.

    5. Wraps both SSH calls in the same defensive pattern as `wait_for_install_complete`
       (never raises on SSH/connection errors) -- if either `runner.exec()` call itself
       hits a connection-level failure (`exit_code == -1`), classify as `("SKIP", "Could
       not verify boot chain: SSH unreachable ({stderr})")` rather than crashing the
       pipeline or silently defaulting to PASS.

    Returns a `(status, reason)` tuple where `status` is one of `"PASS"`, `"WARN"`, or
    `"SKIP"` (never `"FAIL"` -- per the research and Pitfall 4, an unsigned-fallback
    finding is a WARN to be surfaced and judged by the release process, not an automatic
    hard failure of this test run).
  </action>
  <acceptance_criteria>
    - `grep -c "def verify_boot_chain" scripts/ppsa-installer-e2e.py` equals 1
    - `grep -c "secure.boot|shim" scripts/ppsa-installer-e2e.py` is greater than 0
      (dmesg query present)
    - `grep -c "proc/cmdline" scripts/ppsa-installer-e2e.py` is greater than 0
      (fallback query present)
    - `grep -c "Unsigned" scripts/ppsa-installer-e2e.py` is greater than 0
      (WARN classification text present, not silently treated as PASS)
    - `python -c "import ast; ast.parse(open('scripts/ppsa-installer-e2e.py').read())"`
      parses cleanly after this task's edits
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Dev/palworld-self-containing-server && python -c "import ast; ast.parse(open('scripts/ppsa-installer-e2e.py').read())" && echo SYNTAX_OK</automated>
  </verify>
  <done>verify_boot_chain() reuses the existing SshRunner pattern to query dmesg then /proc/cmdline over SSH, classifies the result as PASS (signed markers found), WARN (no markers -- likely unsigned fallback, not auto-failed), or SKIP (SSH unreachable), and never raises.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Heartbeat-aware hang detection in wait_for_install_complete() (BOOT-02)</name>
  <files>scripts/ppsa-installer-e2e.py</files>
  <behavior>
    - Test 1 (heartbeat recent, marker absent): poll loop sees `/run/ppsa-install.activity`
      updated within the grace window (e.g. 2 minutes ago) and `/opt/ppsa/.installed`
      still absent -> loop continues polling (treated as "slow but alive"), does NOT set
      a hang failure reason yet, even if elapsed time is large.
    - Test 2 (heartbeat stale past grace threshold, marker absent): poll loop sees the
      heartbeat file's timestamp is older than the grace threshold (e.g. >5 minutes since
      last update) AND the overall timeout has also elapsed -> returns `(False, elapsed)`
      with `self.last_failure_reason` containing a distinct string identifying this as a
      SUSPECTED HANG (heartbeat-stale), not the existing "SSH never reachable" or "marker
      never appeared" generic reasons.
    - Test 3 (heartbeat file absent entirely, e.g. still in Step 1-2 or old install.sh
      without Plan 07-01's change): poll loop does not crash or misreport -- treats a
      missing/unreadable heartbeat file as "no heartbeat signal available", falling back
      to the existing Phase 6 marker-absent timeout reason (not a hang-suspected reason,
      since there's no heartbeat data to call it a hang from).
    - Test 4 (marker appears, regardless of heartbeat state): returns `(True, elapsed)`
      immediately, exactly as Phase 6 already does -- this task must not change the
      success path's behavior or timing.
  </behavior>
  <read_first>
    - scripts/ppsa-installer-e2e.py lines 558-647 (wait_for_install_complete() in full --
      the EXACT existing polling loop, elapsed-time tracking, and the two existing
      last_failure_reason branches this task extends with a THIRD branch, not replaces)
    - .planning/phases/07-boot-chain-verification-hang-detection/07-01-PLAN.md
      "artifacts_produced" section (the exact heartbeat file contract: path, format,
      readability, update cadence, and the "file absent = not yet at Step 3, not an
      error" semantics this task must respect)
    - .planning/phases/07-boot-chain-verification-hang-detection/07-RESEARCH.md
      "Pattern 3: Heartbeat Timeout Logic in Test Harness" and "Pitfall 2: Heartbeat
      Timeout Triggers False Negatives on Slow Docker Pulls" (the grace-period
      reasoning -- heartbeat staleness threshold must be shorter than the overall
      timeout, and must not fire just because Docker Pull output is momentarily quiet)
  </read_first>
  <action>
    Extend `wait_for_install_complete(self, ssh_target, overall_timeout_seconds)` in
    `scripts/ppsa-installer-e2e.py`:

    1. Add a new module-level constant `HEARTBEAT_STALE_THRESHOLD_SECONDS = 300` (5
       minutes -- matches the research's recommended grace period and this file's
       existing `SSH_STALE_THRESHOLD_SECONDS = 300` convention for consistency) and
       `HEARTBEAT_FILE = "/run/ppsa-install.activity"`.

    2. Inside the existing polling loop (the `while True:` block), on each iteration
       where the `.installed` marker check does not yet succeed, add ONE additional SSH
       query: `cat /run/ppsa-install.activity 2>/dev/null` (timeout 10s), using the same
       `runner.exec()` call already in scope. Track a new local variable
       `last_heartbeat_epoch` (parsed `int(stdout.strip())` on success, `None` if the
       file is absent/unreadable/unparseable -- wrap the `int()` parse in a
       try/except ValueError so garbage content never crashes the loop).

    3. When the overall timeout is reached (the existing
       `if elapsed >= overall_timeout_seconds:` branch), extend the existing
       if/else that sets `self.last_failure_reason` with a NEW first-priority check:
       if `last_heartbeat_epoch is not None` AND
       `(time.time() - last_heartbeat_epoch) > HEARTBEAT_STALE_THRESHOLD_SECONDS`, set
       `self.last_failure_reason` to a string explicitly containing the phrase "SUSPECTED
       HANG" and reporting how long the heartbeat has been stale (distinguishing this
       from the existing two reasons). If a heartbeat exists and is still fresh
       (within the threshold) when the overall timeout fires, keep the EXISTING
       "reachable but marker never appeared" reason (this is legitimately just still
       slow, not a hang -- do not relabel it). If no heartbeat was ever observed
       (`last_heartbeat_epoch is None` throughout), also keep the existing two-branch
       logic unchanged (SSH-never-reachable vs marker-never-appeared) -- this preserves
       Phase 6's exact behavior when Plan 07-01's install.sh change is not present on an
       older image.

    4. Print one additional progress-log line per poll iteration showing heartbeat
       staleness in human terms (e.g. "heartbeat: Ns ago" or "heartbeat: none observed
       yet"), appended to the existing per-iteration progress print, so a human watching
       verbose output can see the heartbeat signal live, not just after the fact.

    Do not change the function's return type, its success path (`(True, elapsed)` on
    marker-found), the `POLL_INTERVAL_SECONDS` cadence, or the existing
    `connection_failed` / `last_successful_contact` tracking -- this task ADDS a third
    distinguishable failure reason to the existing two-reason system, per this file's
    already-proven pattern from Phase 6, and does not restructure the loop.
  </action>
  <acceptance_criteria>
    - `grep -c "HEARTBEAT_STALE_THRESHOLD_SECONDS" scripts/ppsa-installer-e2e.py` is
      greater than 0
    - `grep -c "ppsa-install.activity" scripts/ppsa-installer-e2e.py` is greater than 0
    - `grep -c "SUSPECTED HANG" scripts/ppsa-installer-e2e.py` is greater than 0 (the
      third, distinguishable failure-reason phrase is present, not folded into the
      existing two generic reasons)
    - `grep -c "last_heartbeat_epoch" scripts/ppsa-installer-e2e.py` is greater than 0
    - The 4 scenarios in `<behavior>` are exercised via an inline scripted harness
      (monkeypatched `SshRunner.exec` stubs, following the exact pattern used in Phase
      6's 06-02-SUMMARY.md "Coverage" D2 entry) during this task's implementation, with
      each scenario's expected `last_failure_reason` content (or success return)
      confirmed before moving to Task 3
    - `python -c "import ast; ast.parse(open('scripts/ppsa-installer-e2e.py').read())"`
      parses cleanly
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Dev/palworld-self-containing-server && python -c "import ast; ast.parse(open('scripts/ppsa-installer-e2e.py').read())" && echo SYNTAX_OK</automated>
  </verify>
  <done>wait_for_install_complete() polls /run/ppsa-install.activity alongside the existing /opt/ppsa/.installed check, and on timeout distinguishes THREE failure reasons (SSH-never-reachable, marker-never-appeared-heartbeat-fresh, SUSPECTED-HANG-heartbeat-stale) instead of Phase 6's two, without changing the success path or polling cadence.</done>
</task>

<task type="auto">
  <name>Task 3: Wire verify_boot_chain() and heartbeat-aware timeout into run()'s pipeline</name>
  <files>scripts/ppsa-installer-e2e.py</files>
  <read_first>
    - scripts/ppsa-installer-e2e.py lines 649-719 (run() in full, post Task 1/2 edits --
      re-read after Tasks 1 and 2 land since this task extends the SAME method body)
    - .planning/REQUIREMENTS.md BOOT-01/BOOT-02 definitions and the phase's
      "downstream_consumer" framing (Phase 8 will fold a THIRD result -- smoke-test --
      into whatever structure this task produces; the results list/tuple shape
      established here must stay easy to extend, not require a refactor)
  </read_first>
  <action>
    In `InstallerE2ETester.run()`:

    1. Remove the Phase-6-stub NOTE print that deferred boot-chain verification to a
       later phase and stated it was not yet performed by this script -- this plan
       implements that deferred capability now, so the stub disclaimer is obsolete.

    2. After the existing `wait_for_install_complete()` call succeeds (i.e., inside the
       `if success:` branch that currently appends `("wait_for_install_complete", f"PASS
       ({elapsed:.0f}s)")`), call `self.verify_boot_chain(self.ssh_target)` and append its
       result to the `results` list as `("verify_boot_chain", f"{status}: {reason}")` --
       reusing the exact `(step_name, status_string)` tuple shape already used by every
       other step in `results`, so Phase 8 can append a fourth tuple
       (`("smoke_test", ...)`) later without restructuring this list.

    3. Do NOT call `verify_boot_chain()` if `wait_for_install_complete()` itself failed
       (no point checking boot-chain state on a box that never finished installing) --
       skip straight to the summary in that case, matching the existing
       first-failure-stops semantics already used for the earlier pipeline steps.

    4. Update the `overall_pass` computation (`all(status == "PASS" or
       status.startswith("PASS") for _, status in results)`) so a `verify_boot_chain`
       result of `"WARN: ..."` does NOT flip `overall_pass` to False (per Task 1's design,
       an unsigned-fallback WARN is informational, not a hard failure of the E2E run) --
       but a `"SKIP: ..."` result also should NOT flip `overall_pass` to False (SSH
       became briefly unreachable for the boot-chain check specifically, after the
       install itself already succeeded, is not grounds to fail the whole run). Only a
       status starting with `"FAIL"` should count against `overall_pass`. Implement this
       precisely: a step's status counts as passing for `overall_pass` purposes if it
       starts with `"PASS"`, `"WARN"`, or `"SKIP"` -- only `"FAIL..."` fails the overall
       result. Verify this does not silently swallow the EXISTING FAIL-producing steps
       (create_vm/attach_iso/boot_vm/drive_installer_tui/wait_for_install_complete all
       still use bare `"FAIL: {exc}"` or `f"FAIL: {self.last_failure_reason}"` strings
       already, so this generalization is backward-compatible, not a new leniency for
       existing steps).

    5. In the final per-step summary print loop (`for step_name, status in results:
       print(f"  {step_name}: {status}")`), no changes needed -- the existing loop
       already prints whatever string each step produced, so the boot-chain
       PASS/WARN/SKIP reason and any heartbeat-SUSPECTED-HANG reason are automatically
       visible in the one-line-per-step summary output without additional formatting
       work.
  </action>
  <acceptance_criteria>
    - `grep -c "verify_boot_chain(self.ssh_target)" scripts/ppsa-installer-e2e.py` is
      greater than 0 (wired into run(), not just defined and unused)
    - `grep -c "Phase 7's" scripts/ppsa-installer-e2e.py` equals 0 (the Phase-6 stub
      NOTE referencing "Phase 7's responsibility" is removed, not left dangling
      alongside the real implementation)
    - `grep -c 'status.startswith("FAIL")' scripts/ppsa-installer-e2e.py` is greater
      than 0 (overall_pass logic explicitly keys off FAIL, not a bare PASS-only check
      that would silently fail on WARN/SKIP)
    - `python -c "import ast; ast.parse(open('scripts/ppsa-installer-e2e.py').read())"`
      parses cleanly
    - `python scripts/ppsa-installer-e2e.py --help` still exits 0 after all three tasks'
      edits (no regression to the Phase 6 CLI surface)
  </acceptance_criteria>
  <verify>
    <automated>cd D:/Dev/palworld-self-containing-server && python -c "import ast; ast.parse(open('scripts/ppsa-installer-e2e.py').read())" && python scripts/ppsa-installer-e2e.py --help >/dev/null && echo ALL_OK</automated>
  </verify>
  <done>run() calls verify_boot_chain() after a successful completion-poll, appends its PASS/WARN/SKIP result to the same ordered results list used by every other step, and overall_pass is computed so only a FAIL-prefixed status fails the whole run -- WARN (unsigned fallback) and SKIP (boot-chain check itself unreachable) are informational, not blocking. The Phase 6 stub NOTE is removed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-host -> installed guest (SSH, post-boot boot-chain query) | Script SSHs into the now-installed guest to run dmesg/proc-cmdline queries; guest identity/trust is unverified beyond the existing SSH host-key acceptance already established for the install-completion poll |
| test-host -> installed guest (SSH, heartbeat file read) | Script reads a world-readable timestamp file over the same SSH session; content is untrusted (guest-controlled) but bounded to a single parsed integer |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-04 | Tampering | verify_boot_chain() dmesg/cmdline query results | medium | mitigate | Classification is declarative (PASS/WARN/SKIP), never a cryptographic verification -- a compromised/malicious guest could fake dmesg output, but this is a local disposable test VM the script itself created and controls, not a hostile third party; real Secure Boot enforcement happens on real hardware firmware, out of scope per this phase's ROADMAP goal |
| T-07-05 | Denial of Service | verify_boot_chain() SSH queries hanging the pipeline | low | mitigate | Both SSH calls use explicit `timeout=10`, matching the existing bounded-timeout convention from wait_for_install_complete(); any connection failure classifies as SKIP rather than blocking |
| T-07-06 | Denial of Service | Heartbeat-stale hang-detection misfiring on a genuinely slow (not hung) install | medium | mitigate | HEARTBEAT_STALE_THRESHOLD_SECONDS (300s/5min) is a grace period shorter than but independent of the overall pipeline timeout; a fresh heartbeat always preserves the existing (non-hang) "marker never appeared" reason rather than escalating to SUSPECTED HANG, per Pitfall 2's false-negative-avoidance guidance |
| T-07-07 | Repudiation | Parsing a malformed/garbage heartbeat file value | low | mitigate | `int(stdout.strip())` parse is wrapped in try/except ValueError; a malformed heartbeat is treated as "no heartbeat signal available" (falls back to Phase 6's existing two-reason logic), never crashes the polling loop |
| T-07-SC | Tampering | npm/pip/cargo installs | low | accept | No new package-manager installs in this plan (stdlib-only, matching Phase 6's precedent); package legitimacy gate does not apply |

</threat_model>

<verification>
1. `python -c "import ast; ast.parse(open('scripts/ppsa-installer-e2e.py').read())"` --
   valid syntax after all three tasks
2. `python scripts/ppsa-installer-e2e.py --help` -- exits 0, CLI surface unchanged from
   Phase 6 (no new flags required for this plan)
3. `grep -c "def verify_boot_chain"` equals 1 -- BOOT-01 classification function exists
4. `grep -c "SUSPECTED HANG"` greater than 0 -- BOOT-02's third distinguishable timeout
   reason is present, not collapsed into Phase 6's existing two reasons
5. Inline scripted harness (per Task 2's acceptance criteria) exercising all 4 heartbeat
   scenarios, run during implementation before considering Task 2 complete
6. Manual (requires a real CI-built installer ISO + VirtualBox + Plan 07-01's install.sh
   change baked into that image): hand-run
   `python scripts/ppsa-installer-e2e.py <iso-path> --ssh-target <address>` end-to-end and
   confirm the final summary shows a `verify_boot_chain: PASS|WARN: ...` line and, if a
   Docker-pull stall is deliberately induced, a `SUSPECTED HANG` reason distinct from a
   normal timeout -- this is the phase's ultimate proof, expected as a follow-up manual
   verification pass per this project's established pattern (Phase 6 precedent)
</verification>

<success_criteria>
- BOOT-01: `verify_boot_chain()` classifies the post-install boot path as signed
  shim/GRUB (PASS), unsigned fallback (WARN, not auto-failed), or unverifiable (SKIP),
  reusing the existing SshRunner transport
- BOOT-02: `wait_for_install_complete()` polls the heartbeat file alongside the existing
  marker check and produces a THIRD distinguishable timeout reason (SUSPECTED HANG) only
  when the heartbeat itself has gone stale, never misclassifying a fresh-heartbeat slow
  install as a hang
- `run()` wires both into the existing ordered-results pipeline pattern, with
  `overall_pass` keyed strictly off any `FAIL`-prefixed status (WARN/SKIP are
  informational, not blocking)
- The Phase 6 stub NOTE referencing "Phase 7's responsibility" is removed
- All acceptance_criteria across all three tasks pass
- The results-list shape remains a flat list of `(step_name, status_string)` tuples,
  ready for Phase 8 to append a smoke-test result without a refactor
</success_criteria>

<artifacts_produced>
This plan extends `scripts/ppsa-installer-e2e.py` (created/completed by Phase 6) in
place. Full symbol inventory additions for Phase 8's planner to consume:

**File (unchanged path, extended in place):**
- `scripts/ppsa-installer-e2e.py`

**New module-level constants, this plan:**
- `HEARTBEAT_STALE_THRESHOLD_SECONDS = 300`
- `HEARTBEAT_FILE = "/run/ppsa-install.activity"`

**New symbols, this plan:**
- `InstallerE2ETester.verify_boot_chain(ssh_target)` -- returns `(status, reason)` where
  `status` is one of `"PASS"`, `"WARN"`, `"SKIP"` (never `"FAIL"`)

**Extended symbols, this plan (behavior added, signatures unchanged):**
- `InstallerE2ETester.wait_for_install_complete(ssh_target, overall_timeout_seconds)` --
  now also polls `/run/ppsa-install.activity`; `self.last_failure_reason` can now contain
  a third distinguishable value (heartbeat-stale "SUSPECTED HANG") in addition to Phase
  6's existing two (SSH-never-reachable, marker-never-appeared)
- `InstallerE2ETester.run()` -- now calls `verify_boot_chain()` after a successful
  completion-poll and appends its result to `results`; `overall_pass` now keys off any
  `FAIL`-prefixed status rather than a bare `PASS`-only check

**Results-list shape (stable contract for Phase 8):**
- `results` remains a flat `list[tuple[str, str]]` of `(step_name, status_string)`,
  where `status_string` is one of `f"PASS{...}"`, `f"WARN: {reason}"`,
  `f"SKIP: {reason}"`, or `f"FAIL: {reason}"`. Phase 8 can append
  `("smoke_test", "PASS"|"FAIL: ...")` to this same list and reuse the same
  `overall_pass` FAIL-prefix check without modification.

**Explicitly NOT produced by this plan (reserved for Phase 8):**
- `scripts/ppsa-smoke-test.py` invocation/chaining (TEST-01/TEST-02)
- Any change to the CLI argument surface (no new flags needed for BOOT-01/BOOT-02)
</artifacts_produced>

<output>
Create `.planning/phases/07-boot-chain-verification-hang-detection/07-02-SUMMARY.md` when done
</output>
</content>
