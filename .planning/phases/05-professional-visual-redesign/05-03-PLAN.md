---
phase: 05-professional-visual-redesign
plan: 03
type: execute
wave: 3
depends_on: ["05-02"]
files_modified:
  - docker/webui/app/static/index.html
autonomous: true
requirements: [UI-01, UI-02, UI-04]

must_haves:
  truths:
    - "The Firewall tab's descriptive paragraph and ICMP checkbox label no longer use inline style= attributes; spacing comes from shared classes/tokens"
    - "The Backup tab's empty-state row (\"No backups yet\") includes the added next-step line specified in the Copywriting Contract, styled as a muted centered text row matching the Wi-Fi/dashboard empty-state pattern"
    - "The Wi-Fi tab's security column uses the inline SVG lock/unlock icons from the Plan 05-01 sprite instead of the raw 🔓/🔒 emoji characters"
    - "Backup filenames, Wi-Fi SSIDs, and the Firewall port CSV input all have overflow-wrap/text-overflow handling so a long value does not blow out the 375px phone layout"
    - "Wi-Fi and Backup empty-state table rows are styled with the shared muted-text token, not plain unstyled text"
  artifacts:
    - docker/webui/app/static/index.html (modified: #page-firewall, #page-backup, #page-wifi markup + their emoji-to-SVG icon swap)
  key_links:
    - "Wi-Fi tab's wifiScan() JS template literal (icon selection for security column) is updated in the same change as the markup it renders into, so the emoji replacement doesn't leave a mismatched half-migrated state"
</must_haves>
---

<objective>
Apply the Plan 05-01 design-system tokens/classes and icon sprite to the Firewall, Backup, and
Wi-Fi tab markup: remove ad-hoc inline styles, restyle empty-state rows to match the shared muted
pattern, add the Backup empty-state next-step copy, add long-text overflow handling to
filenames/SSIDs/port inputs, and replace the Wi-Fi security emoji (🔓/🔒) with the inline SVG icons
from Plan 05-01's sprite. This plan does not touch the native confirm()/alert() → .modal migration
(that is Plan 05-04) even though some of these calls live in Wi-Fi's JS — only the tab markup and
the one JS template literal that renders the security icon are in scope here.

Purpose: complete visual application of the shared system across all 5 in-scope tabs (UI-01/UI-02),
and close two UI-SPEC backstop items (long-text, and part of the icon-library requirement) with a
concrete implementation.
Output: Updated `#page-firewall`, `#page-backup`, `#page-wifi` HTML blocks; updated `wifiScan()`
icon-rendering line in the `<script>` block.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-professional-visual-redesign/05-UI-SPEC.md
@.planning/phases/05-professional-visual-redesign/05-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle Firewall tab and add long-text overflow handling for port CSV input</name>
  <files>docker/webui/app/static/index.html</files>
  <read_first>
    docker/webui/app/static/index.html (lines 409-449, `#page-firewall` block)
    .planning/phases/05-professional-visual-redesign/05-UI-SPEC.md ("long-text" and "overflow (viewport)" UI Considerations rows, Spacing Scale table)
  </read_first>
  <action>
    Remove the inline `style="color:var(--text-muted);font-size:0.875rem;margin-bottom:1rem"` from
    the descriptive `<p>` at the top of `#page-firewall` and replace it with `class="text-label"`
    plus a new small utility class `.mb-lg { margin-bottom: var(--space-lg); }` added to the `<style>`
    block — apply both classes: `class="text-label mb-lg"`.
    Remove the inline `style="display:flex;align-items:center;gap:0.4rem;color:var(--text);font-size:0.875rem"`
    from the ICMP checkbox `<label>` and replace with a new class `.checkbox-label` added to
    `<style>`: `.checkbox-label { display: flex; align-items: center; gap: var(--space-xs);
    color: var(--text); font-size: var(--font-body-size); }` — apply `class="checkbox-label"`.
    Remove the inline `style="width:auto"` from the ICMP `<input type="checkbox">` and replace with a
    new class `.checkbox-input { width: auto; }` added to `<style>` — apply `class="checkbox-input"`.
    Add `overflow-wrap: anywhere;` to the existing `.form-group input, .form-group select` rule in
    `<style>` so long values typed into the TCP/UDP port CSV fields wrap instead of causing horizontal
    scroll on the input itself at 375px width — this covers the "long-text" backstop for the port CSV
    input specifically named in UI-SPEC.
    Confirm `.section { overflow-x: auto; }` exists (add it if not already present) so the
    `#fw-rules` textarea/section never forces the page body itself to scroll horizontally — this is
    the mechanism UI-SPEC prescribes for the "overflow (viewport)" consideration.
  </action>
  <acceptance_criteria>
    - Zero `style=` attributes remain inside `#page-firewall` after this task
    - `.checkbox-label` and `.checkbox-input` classes exist in `<style>` and are applied to the ICMP
      label/input respectively
    - `.form-group input, .form-group select` rule includes `overflow-wrap: anywhere;`
    - `.section` rule includes `overflow-x: auto;`
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'checkbox-label' docker/webui/app/static/index.html</automated>
  </verify>
  <done>Firewall tab has no inline styles; ICMP checkbox uses shared classes; port CSV inputs wrap long text; .section allows internal horizontal scroll without ever scrolling the page body.</done>
</task>

<task type="auto">
  <name>Task 2: Restyle Backup tab — empty-state copy, filename overflow, restore card spacing</name>
  <files>docker/webui/app/static/index.html</files>
  <read_first>
    docker/webui/app/static/index.html (lines 299-338, `#page-backup` block; lines 897-906, `refreshBackup()` JS empty-state branch)
    .planning/phases/05-professional-visual-redesign/05-UI-SPEC.md (Copywriting Contract "Empty state body" row for Backup, "long-text" UI Considerations row)
  </read_first>
  <action>
    In `refreshBackup()`'s empty branch (currently
    `tbody.innerHTML = '<tr><td colspan="4">No backups yet</td></tr>';`), update the literal to
    `tbody.innerHTML = '<tr><td colspan="4" class="empty-row">No backups yet — click Backup Now or
    Save-File Backup to create one.</td></tr>';` per the Copywriting Contract's specified next-step
    line. Add a new `.empty-row` class to the `<style>` block:
    `.empty-row { text-align: center; color: var(--text-muted); padding: var(--space-lg) 0; }` so the
    empty state reads as an intentional centered muted row, not a bare left-aligned "-".
    Apply `overflow-wrap: anywhere;` to the `td` rule (or add a `.filename-cell` class applied to the
    backup filename `<td>` in the `status.files.map(...)` template literal) so a long backup filename
    does not force horizontal scroll on the table at 375px — add
    `.filename-cell { overflow-wrap: anywhere; max-width: 0; }` to `<style>` and wrap the filename cell
    output as `<td class="filename-cell">${e(f.name)}</td>` in the template literal (`max-width: 0`
    combined with the table's fixed-fraction column sizing lets `overflow-wrap` actually take effect
    inside a table cell — a bare `overflow-wrap` on an auto-width `td` has no visible effect).
    Remove the inline `style="margin-top:1rem"` from the `<div class="card" ...>` wrapping "Restore
    from File" and replace with a new class `.mt-lg { margin-top: var(--space-lg); }` added to
    `<style>` — apply `class="card mt-lg"`.
  </action>
  <acceptance_criteria>
    - `refreshBackup()`'s empty-state literal contains the exact string
      "No backups yet — click Backup Now or Save-File Backup to create one." with `class="empty-row"`
      on the `<td>`
    - `.empty-row` CSS class exists with centered, muted styling
    - `.filename-cell` class exists with `overflow-wrap: anywhere` and is applied to the filename `<td>`
      in the backup files template literal
    - The "Restore from File" card no longer has an inline `style="margin-top:1rem"` — uses
      `class="card mt-lg"` instead
    - Zero remaining `style=` attributes inside the static `#page-backup` markup block (the dynamically
      generated `<tr>` template literals in JS are allowed to use classes but not inline `style=`)
  </acceptance_criteria>
  <verify>
    <automated>grep -c 'click Backup Now or Save-File Backup' docker/webui/app/static/index.html</automated>
  </verify>
  <done>Backup empty state shows the next-step copy in a centered muted row; long filenames wrap inside their cell; Restore-from-File card spacing uses a token-based class instead of inline style.</done>
</task>

<task type="auto">
  <name>Task 3: Restyle Wi-Fi tab — empty state, SSID overflow, and emoji-to-SVG security icon swap</name>
  <files>docker/webui/app/static/index.html</files>
  <read_first>
    docker/webui/app/static/index.html (lines 366-407, `#page-wifi` block; lines 984-1015, `wifiScan()` JS)
    .planning/phases/05-professional-visual-redesign/05-UI-SPEC.md (Design System "Icon library" row, Copywriting Contract Wi-Fi empty state row — already correct, "long-text" row)
  </read_first>
  <action>
    In `wifiScan()`, find the `secIcon` variable assignment — currently a ternary that assigns one of
    two raw Unicode emoji glyphs (an open-padlock glyph for `'Open'`, a closed-padlock glyph
    otherwise) — combined with its usage `<td>${secIcon} ${e(sec)}</td>`. This must change to use the
    Plan 05-01 SVG sprite instead of emoji: replace the `secIcon` variable assignment with
    `const secIcon = sec === 'Open' ? '<svg class="icon"><use href="#icon-unlock"/></svg>' :
    '<svg class="icon"><use href="#icon-lock"/></svg>';` and keep the template literal usage
    `<td>${secIcon} ${e(sec)}</td>` unchanged (it already interpolates `secIcon` directly, so no
    escaping issue — SVG markup is intentionally not passed through `e()`).
    Update the `wifiScan()` "no networks found" empty-state literal
    (`tbody.innerHTML = '<tr><td colspan="5" class="muted">No networks found. Make sure Wi-Fi
    hardware is available.</td></tr>';`) to use the shared `.empty-row` class from Plan 05-03 Task 2
    instead of the ad-hoc `.muted` class: change `class="muted"` to `class="empty-row"` (copy text
    stays identical — it is already correct per Copywriting Contract).
    Do the same for the `<tr><td colspan="5" class="muted">Scanning…</td></tr>` loading-state literal
    at the top of `wifiScan()` — change `class="muted"` to `class="empty-row"` so loading and empty
    states share one visual treatment (per UI-SPEC's loading-state resolution: restyle with the shared
    muted-text token instead of plain unstyled text).
    Add SSID overflow handling: in the network row template literal, wrap the SSID cell as
    `<td class="filename-cell"><strong>${e(n.ssid)}</strong>${conn}</td>` reusing the `.filename-cell`
    class defined in Task 2 of this plan (same overflow-wrap/max-width:0 mechanism applies to any long
    table cell text, not just backup filenames — this is the shared system point of UI-02).
  </action>
  <acceptance_criteria>
    - `wifiScan()` no longer contains the literal emoji characters 🔓 or 🔒 anywhere in the file
      (`grep -c '🔓' docker/webui/app/static/index.html` and the 🔒 equivalent both return 0)
    - `secIcon` assignment now produces `<svg class="icon"><use href="#icon-unlock"/></svg>` or
      `<use href="#icon-lock"/>` depending on security state
    - Both the "Scanning…" and "No networks found" `<tr>` literals use `class="empty-row"` (not `.muted`)
    - The SSID `<td>` in the network row template literal has `class="filename-cell"`
    - `.muted` class definition itself is not deleted from `<style>` if any other tab still references
      it (check remaining usages before removing; if `.muted` is still used elsewhere leave the CSS
      rule in place, only stop using it in these two Wi-Fi literals)
  </acceptance_criteria>
  <verify>
    <automated>grep -c '🔓\|🔒' docker/webui/app/static/index.html</automated>
  </verify>
  <done>Wi-Fi security column renders inline SVG lock/unlock icons instead of emoji; Wi-Fi loading/empty rows use the shared .empty-row treatment; long SSIDs wrap inside their cell via .filename-cell.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Browser → static file | CSS/markup + one JS template-literal edit (icon rendering); no new input surface, `sec`/`ssid` values already pass through `e()` for HTML-escaping before this plan and continue to do so |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-05-04 | Tampering | Firewall/Backup/Wi-Fi markup | low | accept | Static, build-baked, read-only at runtime |
| T-05-05 | Injection (XSS) | wifiScan() SSID/security rendering | low | mitigate | `secIcon` is a fixed, hardcoded SVG string chosen from exactly two literal values based on a boolean check (`sec === 'Open'`) — no external data flows into the SVG markup itself; `n.ssid` continues to be passed through the existing `e()` HTML-escape helper unchanged, so this task introduces no new unescaped-output path |

</threat_model>

<verification>
1. Load the app, navigate to Firewall tab — description paragraph and ICMP checkbox render with no
   visible layout shift from before; type a long string into the TCP ports field and confirm it wraps
   rather than causing the input to scroll horizontally.
2. Navigate to Backup tab with an empty backup list (or inspect via browser devtools by temporarily
   clearing `#backup-files-body`) — confirm the empty-state row shows the full next-step sentence,
   centered and muted.
3. Navigate to Wi-Fi tab — confirm network rows show an SVG lock/unlock icon (not an emoji glyph) in
   the Security column, and that "Scanning…"/"No networks found" rows are styled identically (muted,
   centered).
</verification>

<success_criteria>
- All three remaining in-scope tabs (Firewall, Backup, Wi-Fi) fully consume Plan 05-01's tokens
- No inline `style=` attributes remain in their static markup blocks
- Emoji security icons fully replaced by the SVG sprite from Plan 05-01
- Empty/loading states share one visual class across dashboard/backup/wifi
</success_criteria>

<output>
Create `.planning/phases/05-professional-visual-redesign/05-03-SUMMARY.md` when done
</output>
