# PPSA Web UI - Python Dependencies
fastapi==0.115.*
uvicorn[standard]==0.32.*
httpx==0.28.*
python-multipart==0.0.*
# Ponytail: removed passlib — passlib 1.7.4 + bcrypt>=4.0 is broken
# (passlib's self-test sends a 73-byte password that bcrypt rejects).
# main.py now imports bcrypt directly with our own _hash_pw/_verify_pw helpers.
bcrypt>=4.0
python-jose[cryptography]==3.3.*
jinja2==3.1.*
docker==7.1.*
